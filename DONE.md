# Completed tasks

Historical records only. Read this file when prior verification is relevant; active work belongs in [TODO.md](TODO.md). Preserve task IDs when moving entries here.

- [x] **WD-109 - GitHub Actions native adapter release artifacts (2026-09-16).** `.github/workflows/release-native.yml` (pre-existing from commit `3c1c74e`) had never been exercised on a real GitHub Actions run before this session; getting a tagged run to actually pass and publish surfaced five bugs unrelated to release packaging itself, none visible from local Windows-only testing: `ty check` fails cross-platform on Windows-only `ctypes`/`subprocess` symbols without an explicit `os.name`/`sys.platform` guard (`scripts/codex_hook_canary.py`, `tests/test_daemon.py`); the `bash` availability probe in `tests/test_benchmark_hooks.py` matched the WSL launcher stub in `System32`, which Windows resolves ahead of Git Bash on `PATH` for any bare command name regardless of `PATH` order; a `writer_lock` open-time race (`src/agent_watchdog/storage.py`) let a `PermissionError` from a concurrent lock-file creation escape `control_lock`'s `WriterBusy`-only retry loop, caught only by real concurrent `daemon start` processes racing on a CI runner; and `macos-13` is fully retired from GitHub's hosted runner images, so the original four-target matrix's Intel-macOS package job queued forever and could never schedule. Building `x86_64-apple-darwin` via cross-compilation on `macos-latest` + Rosetta 2 got packaging and three of four smoke legs green across three further tagged attempts (`v0.1.1`-`v0.1.3`), but the daemon the adapter auto-launches never came up under Rosetta even after ad-hoc code-signing the cross-compiled binary (the diagnosed likely cause, per evidence, but not confirmed to be the actual fix). Given GitHub no longer offers a free Intel-hosted macOS runner at all and Apple Silicon has replaced Intel Macs for several hardware generations, `x86_64-apple-darwin` was dropped from the release matrix and from `native_artifacts.py`'s declared targets rather than debugged further; an actual Intel Mac now gets the same explicit unsupported-target diagnostic as any other undeclared host. The release now ships three targets: `x86_64-pc-windows-msvc`, `x86_64-unknown-linux-gnu`, `aarch64-apple-darwin`. Verification: tag `v0.1.4`, run [35150102140](https://github.com/proprock/watchdog/actions/runs/35150102140) — `package` 3/3, `smoke` 3/3 (installed the packaged artifact with no local Rust toolchain and ran `tests/test_rust_adapter.py` against it on each runner), `publish` succeeded; the resulting release ([v0.1.4](https://github.com/proprock/watchdog/releases/tag/v0.1.4)) carries three target zips plus `SHA256SUMS.txt`, confirmed via the GitHub API. Full local `uv run pytest` (452 passed), `uv run ruff check .`, `uv run ruff format --check .`, `uv run ty check` (also run under `--python-platform linux`/`darwin` to catch the cross-platform gap before pushing), `cargo build --release --locked --manifest-path native/Cargo.toml`, `cargo fmt --check`, `cargo clippy --locked -- -D warnings` all passed before each tag. Evidence: [wd109-release-native.md](docs/evidence/wd109-release-native.md) records all four tagged attempts, the exact GitHub Actions run for each, and why three of them published nothing. WD-110 (native-only hook installation) remains open and gated on this task, now scoped to the same three-target matrix.

- [x] **WD-013 - M3 guidance policy (2026-09-14).** Recorded the M3 decision in [docs/architecture.md](docs/architecture.md) ("M3 guidance policy"): the delivery mechanism (observe-default, per-project opt-in, a global kill switch, a delivery record distinct from an observation with evidence IDs/capability/expiry/cooldown, at most one nudge per blocker until new evidence appears, provider-specific safe delivery boundaries, and `proposed`/`delivered`/`response_observed` kept separate from precision) is specified so a future qualifying rule has a fixed target. Applying the 90% precision release gate to the WD-012 calibration (`wd-012.calibration.v3`, 24 sessions): `repeated_tool_outcome` measured 25.00% precision (2 TP / 6 FP, n=8, not a small-sample edge case); `identical_error` and `repeated_test_failure` had zero observations in the cohort; `diff_oscillation` is checkout-scoped and not yet attributable per session (WD-118). Decision: no rule is selected for M3 delivery; every rule stays observe-only, matching the existing M0-M2 boundary, and the 4 recorded false negatives mean a future precision fix alone would not close recall. Re-opening the gate requires a fresh `scripts/calibrate.py report` run showing a rule at or above 90% with its sample size disclosed. Verification: decision review and `git diff --check` passed; no code, hook, or harness behavior changed.

- [x] **WD-012 - Calibration on real work (2026-09-14).** The frozen 24-session cohort (`wd012-sample.json`) was manually reviewed through `scripts/calibrate.py annotate` against the existing control-inbox `verdict` action in an earlier pass on this branch: 16 sessions `progress`, 6 `slow`, 2 unlabeled; 21 `success` / 3 `abandoned` task outcomes. This session closed the remaining acceptance gap: the report had no `recommendations`, though the WD-012 acceptance text requires one. Added `recommend()` to `scripts/calibrate.py` (`PRECISION_GATE = 0.90`), so `report` states, not just implies, which session-scoped rules clear the release gate; format bumped to `wd-012.calibration.v3`. Regenerated [wd012-calibration.json](docs/evidence/wd012-calibration.json)/[.md](docs/evidence/wd012-calibration.md) read-only against the live store: `repeated_tool_outcome` 25.00% precision (2 TP / 6 FP, n=8); `identical_error` and `repeated_test_failure` observed 0 times; 4 false negatives (slow sessions with no session-scoped finding); hook overhead p50/p95 5.1/13.7 ms in-hook, 810.9/2106.2 ms end-to-end over 4915 of 10839 events. This report is the evidence WD-013 selects rules against. Verification: new `recommend()` unit tests (3) plus the full `tests/test_calibration.py` (37 passed); full offline `uv run pytest` (450 passed); `uv run ruff check .`; `uv run ruff format --check .` (repo-wide check remains blocked only by the pre-existing untracked `uber-efficient-software-factory-codex-claude-research.md`, left untouched); `uv run ty check`; `cargo build --release --locked --manifest-path native/Cargo.toml`; `cargo fmt --manifest-path native/Cargo.toml --check`; `cargo clippy --locked --manifest-path native/Cargo.toml -- -D warnings`; `uv build`; and `git diff --check` all passed. The `report` command is read-only; no annotation was written outside the existing control-inbox path and no provider probe was run.

- [x] **WD-102 - Manual and live evidence gates (2026-09-10).** Added [isolated probe result record v1](docs/evidence/probe-result-record-v1.md), the required sanitized template for every future isolated provider probe. Its evidence classification makes `synthetic`, `local_adapter`, and `live_provider` mutually non-conflatable; it separately records provider dispatch, adapter start/failure, missing callbacks, retries, persisted loss categories, end-to-end delivery/read-back, provider/harness/adapter/OS provenance, and terminal cleanup for every owned hook installation, process, temporary state, and probe task. Recording rules require every nonzero failure or loss to be attributed or marked `unknown`; a direct adapter invocation cannot be promoted to provider dispatch, and zero loss counters do not prove callbacks or delivery. `docs/evidence/README.md` and `docs/integrations.md` make the v1 record mandatory for future isolated probes. Verification: documentation/source/link review and `git diff --check` passed. No provider probe was authorized or run.
- [x] **WD-101 - Test realism and boundary coverage (2026-09-10).** Replaced the fragile CLI command-list assertion with an isolated `--home` subprocess contract that proves help does not create state. The Claude fixture-completeness check now lives with the parametrized behavioral event-to-kind mapping. Added offline subprocess integration coverage for project registration, daemon start/pause, report, and the Python hook -> daemon -> SQLite path with both content capture modes; each test-owned daemon is paused in `finally`. Shell launch coverage round-trips harmless arguments through direct execution and every available host shell (`bash`, `cmd.exe`, PowerShell, and pwsh when present). New tests declare their evidence class and make no provider call; mocked polling and synthetic stream units remain tests, not live evidence. Verification: focused changed-module suite (45 passed); full offline `uv run pytest` (420 passed); `uv run ruff check .`; `uv run ruff format --check .`; `uv run ty check`; `cargo build --release --locked --manifest-path native/Cargo.toml`; `cargo fmt --manifest-path native/Cargo.toml --check`; `cargo clippy --locked --manifest-path native/Cargo.toml -- -D warnings`; `uv build`; and `git diff --check` passed. No provider probe or hook installation was run.

- [x] **WD-116 - Retention as a disk-budget control (2026-09-09).** `content_days = 0` and `metrics_days = 0` now disable only their respective age-expiry paths, including transcript-source expiry for content; quota-pressure eviction, pin protection, and degraded-state loss accounting are unchanged. Configuration accepts zero for defaults and project overrides but still rejects negative values. `config.example.toml` documents that zero retains data only until quota pressure. Tests cover no age deletion for old unpinned content and metrics, unchanged default expiry, and zero-setting quota eviction with a pinned session preserved. Verification: focused retention/config suite (40 passed); final offline `pytest -q` (410 passed); `uv run ruff check .`; task-scoped `uv run ruff format --check src tests`; `uv run ty check`; `cargo build --release --locked --manifest-path native/Cargo.toml`; `cargo fmt --manifest-path native/Cargo.toml --check`; `cargo clippy --locked --manifest-path native/Cargo.toml -- -D warnings`; `uv build`; and `git diff --check` passed. Repository-wide Ruff format remains blocked only by the pre-existing untracked `uber-efficient-software-factory-codex-claude-research.md`, left untouched. No provider probe was run.

- [x] **WD-115 - Move the credential filter to the export boundary (2026-09-09).** The Rust adapter now removes only transport `cwd` and spools raw provider JSON; the Python fallback, native spool drain, inbox canonicalization, and SQLite store no longer sanitize envelopes. Size, payload, quota, and UTF-8 artifact constraints remain in place. `inspection.export_sessions` now sanitizes selected raw stored envelopes before writing `events.jsonl`, and the manifest records that the credential filter was applied at export. The privacy, daemon, native-adapter, Claude-hook, telemetry, and CLI tests now cover raw spool/inbox/SQLite/`sessions show` content and a redacted export. Verification: TDD-focused behavior suite (26 passed); final offline `uv run pytest` (407 passed); `uv run ruff check .`; task-scoped `uv run ruff format --check`; `uv run ty check`; `cargo build --release --locked --manifest-path native/Cargo.toml`; `cargo fmt --manifest-path native/Cargo.toml --check`; `cargo clippy --locked --manifest-path native/Cargo.toml -- -D warnings`; and `git diff --check` passed. Repository-wide Ruff format remains blocked only by the pre-existing untracked `uber-efficient-software-factory-codex-claude-research.md`, left untouched. No provider probe was run.

- [x] **WD-114 - Deferred monetary-estimate design and implementation (2026-09-09).** Added `agent_watchdog.pricing` and `facts_query.cost`: an opt-in list-price estimate over the schema-v6 usage rows, surfaced as a `pricing` block on `agent-watchdog usage --tariffs PATH` (or `[pricing] tariffs` in `config.toml`). Billing semantics were verified first: `output_tokens` already includes reasoning/thinking so it is priced once and `reasoning_output_tokens` is never priced (double count); Codex `total_tokens` is an overlapping sum and is never priced; the four billable classes are `input_tokens`, `output_tokens`, `cached_input_tokens`, and `cache_write_input_tokens`, with a single `cache_write` rate that blends Anthropic's 5m (~1.25x input) and 1h (~2x) cache-creation tiers because v6 does not separate them (documented; a split needs a reader/schema change out of scope). The tariff file is TOML: a top-level `currency` plus ISO-date sections, each a complete rate table effective from that date; a `usage` row is priced from its own `model` and timestamp against the latest section at or before it. Nothing is persisted and no v6 counter is touched -- the estimate is recomputed on every call, so editing the file re-prices history with no migration. Provenance is explicit per group: `cost_estimate` (decimal string), a `by_field` split, `unpriced_tokens` with `unpriced_reasons` (`model_not_in_tariff` / `before_earliest_tariff` / `rate_field_missing`), and `tariff_dates`; a missing or malformed file is an error, never a silent zero. Rates are `Decimal` from quoted strings, output quantized to 6 places. Isolation: new module, `token_usage` untouched, `usage` output byte-identical without the flag. Added `pricing.example.toml` and a commented `[pricing]` stanza in `config.example.toml`; documented in `docs/cli.md` and `docs/storage.md`. Verification: new `tests/test_pricing.py` (formula per billable class, dated-section selection, unpriced reasons, precision, malformed-file rejection, `--tariffs` CLI round-trip, no-flag no-change), full offline `uv run pytest`, `uv run ruff check .`, `uv run ruff format --check src tests`, `uv run ty check`, `cargo fmt --manifest-path native/Cargo.toml --check`, `cargo clippy --locked --manifest-path native/Cargo.toml -- -D warnings`, `cargo build --release --locked`, `uv build`, and `git diff --check` all passed. No provider probe.

- [x] **WD-113 - Live database upgrade and telemetry-analytics baseline (2026-09-09).** Upgraded the local Live instance to the WD-111/WD-112 build per `LIVE.md`: rebuilt the native adapter (byte-identical this cycle, no adapter change) and the wheel, replaced the stable adapter binary, `uv pip install --no-deps --reinstall`ed the wheel into the Live venv, and `daemon stop`/`daemon start` with the documented config, data, and runtime paths. Global hook definitions and native trust records were not touched. The daemon opened the project store on start and ran the v5-to-v6 migration transactionally. Sanitized aggregate verification (read-only, no session IDs or captured content): `PRAGMA user_version` 5 -> 6 and `quick_check` `ok` on both sides; every stored envelope still matches its pre-existing `receipts` fingerprint (0 mismatches over the whole table) and all envelopes stay `schema_version=1`, so the migration changed no envelope byte; `event_facts` has exactly one row per event with no orphans and `events.provider` fully backfilled; the five analysis indexes exist and `EXPLAIN QUERY PLAN` for the time-range, model/effort, tool-cost, and tool-pairing shapes each use their partial index with no `json_extract`; distinct session count unchanged (36). Correlation coverage: `conversation_id_source` {session_id: 4409, thread_id: 1486}, `turn_id_source` {turn_id: 3890, prompt_id: 1632, unavailable: 373}, and all 1486 Codex transcript-usage rows (`thread_id`) resolve to a session that also carries Codex hook rows. Configuration attribution: `model_attribution` {observed: 1594, inherited: 793, unavailable: 3508}, `reasoning_effort_attribution` {observed: 1472, inherited: 322, unavailable: 4101}. Token coverage on `usage` rows: `input_tokens`/`output_tokens` non-NULL on all 1808; `total_tokens` non-NULL on the 1486 Codex rows and NULL on the 322 Claude rows by design; raw per-counter sums are preserved with no synthetic total (the early WD-022b reader already populates Claude usage tokens, ahead of the plan's "expected zero" note). 749 `tool.finish` rows carry `tool_duration_ms`. Daemon health after the upgrade: `alive`, all loss counters zero, database `state: ready` at schema 6; the pre-existing `degraded` condition is unrelated transcript enrichment (stale Codex rollout readers: `transcript_unreadable` / `session_meta_mismatch` / `rollout_line_invalid`), present identically before the upgrade and tracked separately since WD-022a. No provider was invoked and no live provider probe was run.

- [x] **WD-112 - Indexed read-only telemetry comparison surface (2026-09-09).** Added `agent_watchdog.facts_query`, a read-only aggregate layer over the schema-v6 `event_facts` table, plus the `agent-watchdog usage` CLI command that assembles it from a read-only snapshot (`inspection.usage`). `token_usage` sums each raw token counter over `kind='usage'` rows grouped by provider, model, reasoning effort, attribution state, conversation, turn, hour, or day, with an optional `received_at` window; every counter is summed as reported and `total_tokens` stays absent for Claude rather than reconstructed from the parts. `process_efficiency` returns per-`tool_name` finishes / error rate (`hook_event_name='PostToolUseFailure'`) / `tool_duration_ms` p50-p90-max, tools and tool wall-time per turn, `permission_mode` throughput, permission-prompt count, inter-turn latency percentiles, and subagent cost by `agent_type`. `coverage` reports the non-NULL count per projected column with `model`/`reasoning_effort` attribution splits, kept separate from the aggregates. The five v6 indexes were already created by the WD-111 migration; `EXPLAIN QUERY PLAN` regression tests confirm the time-range, model/effort, `tool_name`, and tool-pairing shapes hit `event_facts_usage_time` / `event_facts_usage_dim` / `event_facts_tool_cost` / `event_facts_tool_pair` and use no `json_extract`. A pre-v6 database is an explicit CLI error, not an empty result; the existing read-only CLI/database contract (`PRAGMA query_only`, short-lived snapshot) is unchanged. `docs/cli.md` documents the command; `docs/storage.md` points at the helpers. Verification: new `tests/test_facts_query.py`, `uv run pytest` 364 passed, `uv run ruff check .`, `uv run ruff format --check src tests`, `uv run ty check`, `cargo fmt --manifest-path native/Cargo.toml --check`, `cargo clippy --locked --manifest-path native/Cargo.toml -- -D warnings`, `uv build`, and `git diff --check` all passed. No provider probe was run; the live upgrade remains WD-113.

- [x] **WD-111 - Canonical conversation correlation and telemetry projections (2026-09-09).** Schema v6 makes token use and per-turn process cost queryable without JSON extraction while `events.envelope` stays authoritative. `events` gains one `provider` column; all other projections are a companion `event_facts` table (one row per event, `event_id` primary key), materialized synchronously in `Store._put`, removed with its event in `_delete`, and deliberately kept by `_strip_content` so aggregates outlive content expiry. `event_facts` copies the filter keys `provider`/`kind`/`received_at_us` (integer epoch microseconds)/`session_id`/`turn_id` so analysis is single-table and partial `kind` indexes work, then holds envelope context, configuration (`model`/`reasoning_effort` with `observed`/`inherited`/`unavailable` attribution), `usage`-row token deltas (Codex fills all six; Claude maps cache-read/cache-creation/thinking and leaves `total_tokens` NULL; no synthetic sums), and the process-efficiency fields (`hook_event_name`, `tool_name`, `tool_use_id`, `tool_duration_ms`, `permission_mode`, `agent_type`, `notification_type`) read only from the provider metadata namespace. The canonical conversation key is `events.session_id`; `conversation_id_source` records whether it came from the envelope `session_id` or a Codex usage row's `payload.codex.thread_id`. The turn key is Claude's `metadata.prompt_id` or Codex's `turn_id`; `build_envelope` now also promotes Claude `prompt_id` onto the envelope `turn_id`, changing the canonical document and replay fingerprint for Claude hook events written after the upgrade only. Usage-row model/effort inheritance resolves only from earlier observed rows in the same conversation (same turn first), never cross-conversation, cross-provider, or from a later observation; the v5-to-v6 migration backfills in `rowid` order for determinism, with a space pre-check, and advances `PRAGMA user_version` to 6 only on commit. Five analysis indexes (`event_facts_context`, `event_facts_usage_time`, `event_facts_usage_dim`, `event_facts_tool_pair`, `event_facts_tool_cost`) are created; `docs/storage.md` documents the schema. Projection logic is an isolated pure module (`agent_watchdog.facts`). Live figures that sized the design: read-only inspection of the local instance showed 5,430 events at ~4.9 KB/event all-in, ~400 events/working-hour, v6 adding ~12%; retention keeps a full-time year near ~1.1-1.5 GB against the 2 GiB quota, so the existing 30/180-day windows are unchanged. Verification: new `tests/test_storage_v6.py` (failing migration test written first) plus a Claude `prompt_id` promotion test, `uv run pytest` 356 passed, `uv run ruff check .`, `uv run ruff format --check src tests`, `uv run ty check`, `cargo fmt --manifest-path native/Cargo.toml --check`, `cargo clippy --locked --manifest-path native/Cargo.toml -- -D warnings`, `cargo build --release --locked`, `uv build`, and `git diff --check` all passed. No provider probe was run; the live instance upgrade and its aggregate verification remain WD-113.

- [x] **WD-108 - Safe transcript-enrichment failure diagnostics (2026-09-09).** Transcript-reader failures now use one explicit allowlist, including fixed outer I/O, storage, and invalid-enrichment categories; dynamic exception class names are never emitted as their `error_type`. Enrichment results distinguish newly observed failures from active failures, so persistent reader degradation keeps the affected project in daemon status until recovery while the diagnostic log emits only one record per active project/code pair. The default diagnostic record remains content-free; explicit global `log_detail` uses the established bounded, credential-redacted local-detail policy. Verification: focused transcript, daemon, and telemetry tests passed (45 tests); full offline pytest passed (327 tests in six external-basetemp groups); Ruff lint and source/test format, ty, locked Rust release build, Cargo fmt, locked Clippy with warnings denied, wheel/sdist build, and `git diff --check` passed. Repository-wide Ruff format remains blocked only by the pre-existing untracked `uber-efficient-software-factory-codex-claude-research.md`, which was left untouched. No live provider probe was run.

- [x] **WD-028 follow-up - Complete console suppression coverage (2026-09-08).** Routed the daemon's Git diff fingerprint subprocess through the shared Windows-safe process runner and changed Python/Rust daemon launch to use `CREATE_NO_WINDOW | CREATE_NEW_PROCESS_GROUP`; this closes both the production Git path and the visible daemon-window path that were outside the original pytest-only `Popen` patch. Added regression tests for the analysis runner and daemon creation flags. Verification: the final focused suite passed with 89 tests, and the user confirmed that no test windows appeared. Per `LIVE.md`, rebuilt the native adapter and wheel, updated the stable adapter and machine-local wheel without dependencies, and restarted the single global daemon with the documented config/data/runtime paths; `daemon status` reports `alive: true`, with the pre-existing project `degraded` condition tracked separately. No live provider probe or hook installation was run.

- [x] **WD-107 - Provider telemetry retention (2026-09-08).** The native adapter now removes only transport `cwd` and redacts the full remaining provider JSON before its first durable write to the spool; it no longer semantically projects or discards provider fields. The Python fallback applies the same redaction before envelope construction. The daemon preserves provider metadata, tracks `unknown_fields`, exposes per-field `input.*` availability, and emits one bounded, content-free WARNING per unknown top-level field with only the provider and safe field name. Secret values never enter the spool, database, export, or log; numeric token counters are retained. Reports now provide telemetry field coverage and unknown-field counts without inferring model quality, outcomes, or control state. Documentation inventories known signals, provider gaps, and the 20-50 session calibration requirement for WD-012. Verification: new adapter-to-daemon-to-export telemetry contract tests (3 passed), Rust adapter contract tests (40 passed), Ruff lint/source-test format, ty, Cargo fmt, and locked release Clippy with warnings denied passed. The repository's full pytest run was attempted in bounded external basetemp groups because the standard temporary directory has a pre-existing ACL issue; interrupted test-run child daemons were identified and stopped, and no live provider probe was run.

- [x] **WD-106 - Project summary and friendly aliases (2026-09-08).** Added the read-only `agent-watchdog summary` command with per-project database state, distinct provider/session counts, all-event counts, observed cross-project totals, and explicit partial-data/error status. CLI project aliases are the literal repository-folder basenames in registration order; exact duplicate basenames receive `repo`, `repo-2`, and so on, while case is preserved and significant. Aliases select projects across project mutations and inspection/control commands; UUID input remains compatible but normal command-level responses emit aliases. UUIDs remain unchanged in storage, daemon control, raw envelopes, and export artifacts. Verification: focused CLI/observation/registry tests (27 passed); full offline pytest suite (295 passed); full Ruff lint, source/test Ruff format check, ty, wheel/sdist build, locked Rust release build, Cargo fmt, locked Clippy with warnings denied, and `git diff --check` passed. The repository-wide Ruff format check remains blocked only by the pre-existing untracked `uber-efficient-software-factory-codex-claude-research.md`, which was left untouched. No live provider probe or hook installation was run.

- [x] **WD-104 - Privacy-safe daemon logs (2026-09-08).** Added a bounded, best-effort Python-core diagnostic log at `<data>/watchdog.log`. It rotates under an inter-process lock to numbered archives; `log_files` counts the active file, `log_bytes` caps each file, and global `[defaults] log_level` accepts `DEBUG`, `INFO`, `WARNING`, or `ERROR` (default `INFO`). Per-project log overrides were removed because one daemon-wide log cannot safely inherit a project policy. The allowlisted line format carries only fixed component/event/decision/reason/error codes, nonnegative counts/byte sizes, and local Watchdog project/event UUIDs. Prompts, paths, commands, payloads, provider/session IDs, tool input/output, exception messages, and tracebacks are rejected by construction. Daemon spool/control/lifecycle/poll paths, CLI failures, and the Python hook fallback now distinguish routine DEBUG decisions, lifecycle/control INFO, recoverable WARNING, and contained ERROR conditions without affecting stdout/stderr or fail-open behavior. Rust adapter diagnostics remain unchanged. Verification: new rotation, level, concurrent-write, global-config, daemon-spool, and hook-privacy tests; focused and full offline pytest; Ruff lint/format, ty, wheel/sdist build, locked Rust release build, Cargo fmt, locked Clippy with warnings denied, and `git diff --check` passed. No live provider probe or hook installation was run.

- [x] **WD-105 - Trusted Git project auto-registration (2026-09-08).** Added opt-in `auto_add_projects` (default false) and `trusted_projects_dir` configuration. The trusted directory expands `~`, is stored as a canonical absolute path, and must exist when auto-add is enabled. An unknown event now atomically registers and persists only a Git checkout whose canonical root is inside that directory; non-Git and outside paths remain unregistered. Explicit `project add` behavior is unchanged. The Python hook supports an auto-add-only configuration with default payload limits, while daemon spool draining applies the same policy to native-adapter records. Verification: new configuration, registry/worktree, Python-hook, daemon-spool, and native-adapter tests; full offline pytest suite (284 passed); Ruff lint and source/test format checks; ty; wheel/sdist build; locked Rust release build, Cargo fmt, and locked Clippy with warnings denied; `git diff --check` passed. The repository-wide Ruff format check is blocked only by the pre-existing untracked `uber-efficient-software-factory-codex-claude-research.md`, which was left untouched. No live provider probe or hook installation was run.

- [x] **WD-103 - Software factory alignment audit (2026-09-07).** Audited revision `1360dce` against the user-supplied research article, accepted architecture, implementation paths, test suite, Git history, and saved live/synthetic evidence. The [audit report](docs/software-factory-audit.md) concludes that the external observer, durable collection, and deterministic shadow-analysis direction is sound, but progress measurement, real-session calibration, guidance, semantic judging, human escalation, benchmark-proven model routing, and skill improvement are not yet implemented or empirically justified. It also records narrower implementation boundaries: the Python fallback bypasses the native spool path, production ingestion does not use the available artifact offload, and Git diff fingerprints omit staged and untracked state. WD-012 now requires auditable progress/finding labels and false-negative accounting; WD-101/102 define practical test and manual/live evidence follow-ups. Verification: locked Rust release build passed; 131 focused offline tests and the full 278-test suite passed with basetemp outside the repository; direct and PowerShell synthetic native benchmarks each delivered 41 events across restart with zero losses. The benchmark reports explicitly set `native_provider_validation` to false. An initial repo-local basetemp run exposed Git-discovery sensitivity in the test harness and was rerun under the corrected external condition. `git diff --check` passed. No hooks were installed and no new Codex, Claude, other-OS, or remote CI probe was run. Two test-owned basetemp directories remained under the OS temporary directory after verified cleanup attempts, including an elevated retry, returned access denied; no repository files were placed there.

- [x] **WD-011 - Labels, pin, and manual export (2026-09-07).** Added provider-scoped session outcomes/types in storage schema v5 and the `label`, `pin`/`--unpin`, and `purge` CLI commands. Mutations are bounded request files processed and durably acknowledged by the sole core writer; the CLI never opens a write connection. `export` is an offline read-only snapshot for explicitly selected sessions and creates `events.jsonl`, `summary.md`, `manifest.json`, and `manual-prompt.md` in a new directory. The manifest records `wd-011.v1`, labels, counts, gaps, redaction limits, and the mandatory manual content review before external sharing. Purge deletes only selected provider/session Watchdog rows, unreferenced artifacts, labels, eligible pins, and Watchdog transcript-reader state; it never reads or modifies vendor transcripts or project files. Verification: 278 offline pytest tests in four bounded groups (56 analysis/provider contracts, 79 daemon/hook contracts, 104 remaining Python storage/inspection tests, 39 Rust-adapter tests), focused post-change CLI/storage/export checks (41), Ruff lint/format, ty, `uv build`, locked Rust release build, `cargo fmt --check`, locked Clippy with warnings denied, and `git diff --check` passed. No live provider probe was run.

- [x] **WD-010 - Shadow findings and reports (2026-09-07).** Added the read-only `report` CLI command with a provider/session snapshot and deterministic `wd-010.v1` shadow rules. Reports expose received-event timeline, wall and paired tool durations, output-size/outcome metrics, compaction and usage deltas, explicit gaps, evidence event IDs, and versioned findings. Repetition requires three matching observations: the exact tool input/outcome, structured failing result, or comparable pytest/JUnit failing set. Successful exit codes, waiting records, and Stop do not claim progress, a stall, or task success. Schema v4 stores only a debounced SHA-256 Git diff fingerprint and byte count per checkout; the core takes that bounded read after spool admission, never in a hook, and A-to-B-to-A findings explicitly use uncertain attribution. Verification: 276 offline pytest tests in three bounded batches (60 core, 149 contracts/hooks/transcripts/retention, 67 adapter/tooling), focused post-change analysis tests (3), Ruff lint/format, ty, `uv build`, locked Rust release build, `cargo fmt --check`, locked Clippy with warnings denied, and `git diff --check` passed. No live provider probe was run.

- [x] **WD-028 - Suppress console windows from the offline test suite on Windows (2026-09-07).** The suite spawned console-subsystem children (`git`, `python`, `powershell`, the native adapter) with no window flags, so a full `uv run pytest` from a windowless parent flashed dozens of console windows. Added `agent_watchdog._proc` with `hidden_creationflags()` plus `run`/`popen` wrappers that OR `CREATE_NO_WINDOW` into `creationflags` on Windows unless the caller already selects `DETACHED_PROCESS`/`CREATE_NEW_CONSOLE` (redirected stdio, pipes, and timeouts are unaffected). `registry.discover()` routes its `git rev-parse` fallback through the wrapper; the standalone scripts (`benchmark_hooks`, `codex_hook_canary`, `hook_stream_timing`, `detach_probe`) inject the flag at their non-detached spawn sites. New `tests/conftest.py` monkeypatches `subprocess.Popen.__init__` for the whole session, enabled by default and disabled by setting `WATCHDOG_TEST_CONSOLE_WINDOWS` to `1`/`true`/`yes`/`on`. The daemon-launch paths (`daemon.py`, `native/src/main.rs`) already detach and are unchanged. Branch fast-forwarded onto WD-009. Verification: `uv run pytest` 272 passed, and a 57-test `rust_adapter`/`detach_probe`/`daemon` subset passed with `WATCHDOG_TEST_CONSOLE_WINDOWS=1`; Ruff lint/format (126 files), ty, `uv build`, `cargo fmt --check`, `cargo clippy --locked -- -D warnings`, `cargo build --release --locked`, and `git diff --check` all passed. Runs used an alternate `--basetemp` because the pre-existing `%TEMP%\pytest-of-tonym` directory has an unreadable ACL from an earlier session (unrelated to this change). No live provider probe.

- [x] **WD-009 - Codex transcript enrichment (2026-09-07).** Added the daemon-only, version-gated `codex-rollout-v1` reader. It registers an absolute `transcript_path` only from a normal Codex hook envelope, validates `session_meta` against that session, then accepts only verified `token_usage_record` records. The durable schema-v3 source state retains file identity, byte offset, partial UTF-8 tail, canonical thread counters, and the last reported reader failure; observed paths expire after 30 days without touching vendor files. Append, partial tail, truncation/replacement, malformed/unsupported format, and counter regression are explicit, deduplicated `observation.gap` outcomes. Each accepted record becomes an idempotent usage envelope correlated by session/turn/response, with `thread_token_usage` as the only cumulative scope and separate observed/unavailable input/output/cached fields. Transcript text, raw records, prompts, and tool output are never persisted; numeric usage metadata remains available even when content capture is disabled. Session inspection now reports `usage_not_enriched` only until an accepted provider/session usage event exists. Verification: 271 offline pytest tests passed in five batches, Ruff lint/format, ty, wheel/sdist build, `cargo fmt`, locked Clippy with warnings denied, locked release build, and `git diff --check`. No live provider probe was run.

- [x] **WD-027 - Spool-and-forget adapter (2026-09-06).** The native adapter no longer parses `config.toml`, compiles redaction regexes only to build the envelope, resolves the checkout, or walks the project data tree. It now reads JSON stdin, redacts a fixed field projection (`hook_event_name`, `tool_name`, `tool_use_id`, `session_id`, `turn_id`, `agent_id`, `tool_response`, `prompt`, `tool_input`, `last_assistant_message`), and atomically writes one record (projection plus raw `cwd`, adapter-stamped `event_id` and `received_at`) under `<data>/spool/`. Checkout resolution, envelope construction (shared `agent_watchdog.hooks.build_envelope`), per-project payload/inbox/project quota, and inbox admission moved to a new `daemon._drain_spool`, run once per `_poll`. The daemon publishes `<data>/spool/limits.json` (largest registered payload limit, spool cap) on config-mtime change and at launch; absent, the adapter falls back to 1 MiB payload / 64 MiB / 4096-file caps. `native/src/config.rs` and the `toml` crate were removed; `disk::usage`/`disk::room` recursion replaced by a flat `spool_footprint`. Semantics changes: an unregistered or since-removed `cwd` is discarded at drain (async fail-open); a payload over the global cap counts `payload` on `<data>`, over a stricter project's limit counts it on the project at drain; `event_id`/`received_at` are minted by the adapter and trusted by the daemon (replayed spool records dedupe in `Store`). Invariants held: hooks never block on the daemon, never inject context, fail open; secrets removed before the first durable write (the spool). Synthetic launch cost: concurrent four-caller p95 96.4 -> 65.2 ms, first call 43.6 -> 33.4 ms, sequential p95 unchanged at the spawn floor (41/41 events, zero losses, restart preserved). Verification: `uv run pytest` 260, Ruff lint/format, ty, `uv build`, `cargo fmt`/`clippy --locked -- -D warnings`/`build --release --locked`, `git diff --check`. Evidence: [wd027-spool-bench.json](docs/evidence/wd027-spool-bench.json). Windows shell-launch latency and the Codex runner remain WD-026; no live provider probe.

- [x] **WD-024 - Lightweight Rust adapter (2026-09-06; closed with external limitations by user decision).** Implemented the native adapter while retaining the Python core/inbox contract, with cross-language, installed-binary, privacy, pause, allowlist, quota, loss, lock, and worktree coverage. The shared checkout-resolution optimization reduced the adapter's synthetic direct Rust p95 to 64.6 ms sequential / 173.0 ms four-way (20 samples, 41/41 events, zero losses, restart preserved). The actual standalone Codex CLI 0.153.3 PowerShell hook contract was captured and then verified by the console canary; a nested CMD `commandWindows` variation failed before handler start, so there is no demonstrated direct-CMD runner selection. The later actual PowerShell recheck improved to 278.7/563.3 ms but did not meet the original 250 ms SLO. Remaining Codex concurrent callbacks and unavailable desktop lifecycle triggers are recorded as provider limitations, not claimed as supported. The user accepted closure without claiming that the original provider-controlled latency or unavailable trigger gates passed; WD-026 owns a future Windows Codex hook-performance resolution. Verification: 20 canary/benchmark tests, Ruff lint/format, ty, `git diff --check`; historical Rust adapter and live evidence remain in [verification](docs/verification.md#wd-024-rust-adapter-closed-with-external-limitations), [direct recheck](docs/evidence/wd024-windows-direct-fast-recheck.json), and [blocker attribution](tmp-WD-024.md).

- [x] **WD-022a - Claude observation (2026-09-06).** Provider-parameterized Python and Rust adapters, a `settings.json` / `settings.local.json` installer emitting exec-form entries with `timeout: 2`, the 12-event Claude map, empty-stdout no-op on every path, `provider` in the ownership record with legacy-Codex read compatibility, and a provider-aware launch benchmark. Content capture stays the four Codex-equivalent fields; enrichment, wider capture, lifecycle beyond observation, and guidance/control are WD-022b. Part 2 fix after the first live pass: checkout identity now resolves from filesystem reads (`.git`, worktree `gitdir:`, `commondir`), spawning `git rev-parse` only for unrecognized layouts, proven byte-identical to `git` across six layouts in `tests/test_checkout_resolution.py` and cross-language in `tests/test_rust_adapter.py`; `disk::fault` adds a rotating, content-free `faults.log` (`<timestamp> <event> <reason-category>`) so any nonzero `invalid` counter is attributable. Removing the per-hook subprocess halved the launch benchmark (direct exec four-caller p95 153 -> 96 ms). Verification: full offline suite green (`uv run pytest` 242, Ruff lint/format, ty, `uv build`, `cargo fmt`/`clippy`/`build --release`). Live gate met on Windows: Stage 1 interactive `hookInfos.durationMs` present for `Stop` (51-71 ms); Stage 3 four concurrent `claude -p` (`--model claude-haiku-4-5`) stored a complete 8-event set per session with **no dropped `SessionStart`** (the pre-fix defect did not reproduce), all loss counters zero across a daemon restart, 36/36 event ids preserved, no `faults.log`, `outcomes {success: 28}`, 8/8 `tool_use_id` -> `tool.finish`; Stage 4 supervised desktop pass (Claude Code 2.1.263, project-local `settings.local.json`) had a real composer submission produce a stored `turn.start`, plus SubagentStart/Stop, `/compact`, `PostToolUseFailure`, and `SessionEnd` on archive, 6/6 `tool_use_id`s paired, 38 events, zero losses. 11 of 12 native events observed live (only `Notification` uncovered; `Interrupt` does not exist in the build). Isolation held: no Watchdog code writes under `~/.claude`; the isolated daemon was `alive=false` at each teardown; hooks uninstalled. Criterion 1 is judged on Watchdog's own blocking cost (launch benchmark four-caller p95 96.4 ms), which by the plan's recorded limitation excludes Claude's own hook-dispatch overhead — not exposed on this build and not Watchdog's cost. Evidence: [verification](docs/verification.md#wd-022a-claude-observation-gate-met), [launch](docs/evidence/wd022a-claude-launch.json), [aggregate](docs/evidence/wd022a-windows-claude.json). No ordinary chats or Cowork; macOS/Linux stays WD-019.

- [x] **WD-008 - Observation CLI and measured baseline (2026-09-06).** Added JSON project add/list/remove/relocate, doctor, and read-only sessions list/show with bounded pages and explicit missing lifecycle/outcome data. Added an opt-in synthetic benchmark: 81 events across two worktrees/session identities survived restart without recorded loss; p95 was 1089.5 ms sequentially and 1505.7 ms with four callers. Native CLI probes exposed two-second hook timeouts after correcting scratch Git ownership. The user explicitly moved target latency and the remaining concurrent CLI/desktop/event matrix to Rust adapter WD-024 before WD-009; these checks did not pass in WD-008. Verification: 148 pytest tests, Ruff lint/format, ty, wheel/sdist build, and installed-wheel project/doctor/session/capture smoke passed. Corrected the smoke's table-readiness polling race. Probe hooks were removed, TUIs exited, tasks archived, and owned daemons stopped. See [CLI](docs/cli.md) and [verification](docs/verification.md).

- [x] **WD-025 - Avoid holding the diagnostic lock during disk sync (2026-09-06).** A WD-008 regression exposed slow fsync blocking another counter update. Keep the unbuffered read/modify/write under the lock, then fsync with the handle still open after releasing the lock. A deterministic delayed-fsync pytest failed before the change and passed afterward. Corrected the older contention test to verify all successful increments and explicitly allow reported diagnostic-write failures under the existing best-effort timeout contract. Verification: 17 focused storage/CLI cases and the full 148-test suite passed; Ruff and ty passed. No timeout was increased and no audit-ledger durability claim was added.

- [x] **WD-007 - Retention, quotas, redaction (2026-09-06).** Added default-on text capture with global/project opt-out, known-credential redaction before persistence, metadata-only rejection diagnostics, 30/180-day retention, session pins, project/inbox/payload quotas, reserve margin, and fixed-size persistent loss counters exposed by daemon status. Added schema v2 migration, replay fingerprints that survive content expiry, orphan/stale temporary cleanup, incremental vacuum, and WAL checkpointing. No new dependency. Verification: behavioral tests failed before implementation; 140 pytest tests, Ruff lint/format, ty, wheel/sdist build, and isolated installed-wheel hook-to-daemon-to-SQLite capture/redaction smoke passed on Windows. The smoke daemon was stopped. Tests cover concurrent rejection accounting, pinned saturation, WAL/auxiliary accounting, physical reclamation, v1 upgrade, and simulated disk-full publication. Redaction and counter durability have explicit operational limits; quotas are cooperative and reserve is an admission margin. Vendor transcripts are never cleanup targets. Other-OS hosts remain WD-019; WD-011 later delivered the pin CLI. See [storage](docs/storage.md) and [verification](docs/verification.md).

- [x] **WD-006 - Codex hook adapter and installer (2026-09-06).** Added bounded metadata-only observation for 11 native events, exact JSON no-op output, pause/allowlist checks, short Git identity lookup, and detached daemon recovery. Added explicit JSON install/uninstall with dry-run default, exact ownership records, backups, interrupted-write recovery, and preservation of user hooks. No new dependency. Verification: TDD red with missing modules; 123 pytest tests, Ruff lint/format, ty, wheel/sdist build, and an isolated installed-wheel hook-to-daemon-to-SQLite check passed. Live CLI success/failure/Stop/exit and desktop resume/tool/Stop were recorded after native trust review in the correct profile. Fresh desktop load after uninstall added no events. Probe processes were stopped and tasks archived. Remaining desktop event coverage and latency measurement remain WD-008; content/redaction/quotas remain WD-007; other-OS hosts remain WD-019. See [hooks](docs/hooks.md) and [verification](docs/verification.md).

- [x] **WD-005 - Daemon lifecycle (2026-09-05).** Added detached start/run/stop/pause/status commands, OS-backed singleton ownership, persistent pause with request IDs and acknowledgments, bounded polling, serialized registry mutations, and pause/allowlist-aware event admission. No new dependency or general command framework. Verification: 81 pytest tests passed, including independent concurrent CLI launches, crash recovery using process-local `os._exit`, stale PID/heartbeat, immediate stop/start, registry contention, and degraded project recovery; Ruff lint/format, ty, wheel/sdist build, and isolated installed-wheel start/stop with a spaced path passed. Live Codex CLI exit and desktop task unload preserved the daemon; the desktop instance committed a new synthetic event after unload. The user accepted desktop task unload as sufficient for WD-005; full desktop application shutdown was not tested. All probe daemons were stopped and tasks archived. No live hook installation or other-OS/remote CI claim. See [daemon contracts and evidence](docs/daemon.md).

- [x] **WD-023 - Detach probe cleanup race.** During WD-003 verification, the offline probe failed with Windows error 32 because its child inherited the temporary working directory. Launch the child from the parent directory and use explicit typed process flags. Verification: reproduced failure, then focused pytest passed and ty passed. No live provider probe was run for this fix.

- [x] **WD-018 - Bounded Windows detach and Claude startup probe (2026-09-05; split from WD-002).** Added an offline process-lifetime test and opt-in Claude init-only probe using temporary settings. Red: missing probe script; green: detached child responded after parent exit. Live Claude Code 2.1.259 SessionStart and child survival passed outside the execution sandbox without a model conversation. The restricted run was inconclusive. Evidence and remaining provider gaps are in [the compatibility report](docs/provider-compatibility.md); WD-002 remains open.

- [x] **WD-001 - Foundation (2026-09-05).** README/AGENTS/CLAUDE, ROADMAP, architecture/integration contracts, uv package/lock, CLI help, and CI matrix. Local Windows/Python 3.12.13: 2 tests passed without warnings using an accessible cache; locked sync, Ruff check/format, wheel/sdist build, and CLI help passed. The built wheel also installed and ran in an isolated environment. M0 acceptance was audited after the documentation update; details are recorded in [verification](docs/verification.md). macOS/Linux/Windows CI is configured but has not run remotely; live hooks have not been tested.
- [x] **WD-017 - English repository text and separate completion history (2026-09-05).** Translated repository documentation into English, made English mandatory in AGENTS.md, and moved completed tasks out of TODO.md into DONE.md. Verification: reviewed translated contracts and task IDs; checked repository text for remaining Cyrillic, local Markdown links, UTF-8/LF, whitespace, and `git diff --check`. No runtime behavior changed.

- [x] **WD-020 - Defer other-OS compatibility acceptance to M5 (2026-09-05).** Moved macOS/Linux host access and live compatibility verification to WD-019 in the final milestone. Removed those dependencies from WD-002 and M1/WD-008, retained portable design and the existing CI matrix, and synchronized architecture and verification records. Verification: reviewed scope references and local links; `git diff --check` passed. No runtime behavior changed.

- [x] **WD-021 - Explicit pytest requirement (2026-09-05).** AGENTS.md now requires pytest for tests, including fixtures, parametrization, and plain assertions, and keeps live provider probes separate. Verification: documentation review and `git diff --check`; no runtime change.

- [x] **WD-002 - Provider compatibility investigation (2026-09-05).** Closed with user-accepted limitations after the Codex-first scope decision. Live hook shapes for both CLIs and desktop coding paths, a bounded capture probe, and sanitized evidence are recorded in [the report](docs/provider-compatibility.md). Prior implementation verification: 8 pytest tests and Ruff passed in 838ecec; hooks were disabled and probe tasks archived. Codex lifecycle validation remains WD-005, remaining Codex desktop events WD-008, and Claude implementation/validation WD-022 after WD-014 and before WD-015. Untested behavior is not claimed as supported. Closeout verification: reviewed plan dependencies, evidence references, English text and local links; `git diff --check` passed.

- [x] **WD-003 - Config, registry, and event envelope.** Added strict versioned TOML configuration with defaults/project overrides, platformdirs paths, explicit registration/removal/relocation, shared Git common-dir identity for worktrees, and a provider-neutral JSON envelope with nullable native IDs. Added ty to development checks and CI, documented the library API and small-utility scope. TDD red: missing config/registry modules; final green: 42 pytest tests, Ruff lint/format, ty, locked sync, wheel/sdist build, and isolated installed-wheel contract imports passed on Windows. Real temporary Git worktree/clone, symlink, path casing, relocation, invalid config, and failed atomic-save scenarios passed. CI configuration is not a remote CI run. CLI commands, concurrent registry mutation coordination, daemon, storage, and adapters remain later tasks. See [contracts](docs/contracts.md).

- [x] **WD-004 - Inbox and project storage.** Added atomic envelope publication, bounded batch ingestion/quarantine, one OS-locked SQLite writer per project, WAL with an atomic initial schema migration, UUID replay/conflict checks, and content-addressed artifacts with transactional references. Persisted records require explicit replay identity; corrupt input does not stop valid records. Verification: TDD began with the storage module absent; 68 pytest tests (26 new storage cases), Ruff lint/format, ty, wheel/sdist build, and isolated installed-wheel storage smoke passed on Windows. Test-owned processes crashed before/after event COMMIT and before migration COMMIT, then recovered without duplicate events or retained writer locks. Future schemas and wrong project bindings were refused. No new dependencies, live provider calls, daemon, quota/retention system, or remote CI run. See [storage contracts](docs/storage.md).
