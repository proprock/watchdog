# TODO

Queue: WD-111 -> WD-112 -> WD-113 -> WD-012 -> WD-101/WD-102 -> WD-013 -> WD-014 -> WD-022b (M-Anthropic) -> WD-015 -> WD-019 -> WD-026 -> WD-109 -> WD-110 -> WD-114. WD-009, WD-010, WD-011, WD-022a, WD-024, WD-027, WD-103, WD-107, and WD-108 are complete (see [DONE.md](DONE.md)). WD-016 is optional; WD-026 is the final deferred provider-performance task. M1-M4 support Codex coding only; ordinary chats are excluded. Design: ROADMAP.md and docs/architecture.md. The [WD-103 audit](docs/software-factory-audit.md) records the evidence boundary and follow-up priorities. This file contains open work only; completed entries move to [DONE.md](DONE.md) with their IDs and verification evidence. Pending work has not yet been verified.

## M2 - Analysis

- [ ] **WD-012 - Calibration on real work.** Before calibration, extend the provider-scoped session labels with `progress`, `slow`, `stuck`, and `externally_blocked`, plus a per-finding verdict of `true_positive`, `false_positive`, or `uncertain`. Then manually label a target of 20-50 real sessions, including sessions with no emitted finding so false negatives can be counted. Measure finding precision, false positives, false negatives, overhead, and the distribution of task/session states. Depends on WD-011 and is the evidence gate for WD-013. Acceptance: an auditable report with the dataset definition, counts, confusion table, limitations, and recommendations, without automatic harness changes.

- [ ] **WD-111 - Canonical conversation correlation and telemetry projections (Codex + Claude).** Implement the schema-v6 portion of [the database upgrade plan](db-upgrade-plan.md): materialize queryable envelope context, canonical conversation/turn identifiers, direct model/effort observations, validated transcript usage deltas, and the Claude process-efficiency projections, while retaining `events.envelope` as the authoritative document. Correlate Codex hook `session_id` with transcript `thread_id`. For Claude, take `conversation_id` from the envelope `session_id` (`conversation_id_source='session_id'`) and `turn_id` from `payload.claude.metadata.prompt_id` (`turn_id_source='prompt_id'`); backfill `hook_event_name`, `tool_name`, `tool_use_id`, `tool_duration_ms`, `permission_mode`, `agent_type`, and `notification_type` from the metadata namespace; read Claude `reasoning_effort` from the nested `effort.level` and mark it `observed`. Claude token/`model` columns exist but stay `NULL` (`model_attribution='unavailable'`) until WD-022b. Resolve usage configuration only from the same conversation and matching/earlier turn context, with explicit provenance. Do not read model/effort from captured tool content, use cross-conversation defaults, or infer unavailable values. Migrate v5 data transactionally in insertion order without changing envelopes, replay fingerprints, artifacts, or retention behavior. Acceptance: migration, rollback/retry, idempotence, Codex and Claude correlation, model-change, mixed-provider isolation, and unavailable-value tests pass; no provider probe is authorized.

- [ ] **WD-112 - Indexed read-only telemetry comparison surface.** Add and document the v6 context, partial usage, and partial tool indexes from [the database upgrade plan](db-upgrade-plan.md), plus read-only SQL/query helpers for time-, model-, effort-, attribution-, conversation-, and turn-based token comparisons and for the Claude process-efficiency templates (tools per turn, tool wall-time per turn, tool error rate via `hook_event_name='PostToolUseFailure'`, `tool_duration_ms` percentiles by `tool_name`, inter-turn human latency, permission-stall count/time, throughput by `permission_mode`, subagent cost by `agent_type`). Aggregate only raw reported values; preserve coverage separately and never synthesize a total from overlapping input/cache/output/reasoning fields. Acceptance: the time-range, model/effort, tool-pairing, and `tool_name` query shapes use the intended indexes under `EXPLAIN QUERY PLAN`, avoid `json_extract` in normal analysis, and preserve the existing read-only CLI/database contract.

- [ ] **WD-113 - Live database upgrade and telemetry-analytics baseline.** After WD-111 and WD-112 pass their offline checks, update the local Live instance according to `LIVE.md`, migrate its existing database (Codex and Claude rows), and record a sanitized aggregate-only verification of schema version, envelope preservation, Codex hook-to-transcript correlation coverage, Claude `session_id`/`prompt_id` correlation and process-efficiency projection coverage, unavailable model/effort coverage, token-counter coverage (expected zero for Claude until WD-022b), and query-plan results. Do not invoke a provider, expose session IDs or captured content, alter global hook configuration, or claim a live provider probe. Acceptance: daemon health is restored after the upgrade, the aggregate verification is reproducible, and all changes to the live instance follow the documented wheel/daemon procedure.

- [ ] **WD-114 - Deferred monetary-estimate design and implementation.** Scheduled after the token-only v6 upgrade and a useful token-analytics baseline. Verify the provider billing semantics for cached input, cache writes, output, and reasoning tokens before proposing any formula; then make a separate decision for tariff source, configuration/versioning, historical-rate handling, currency precision, and estimate provenance. Never treat subscription usage as measured spend, overwrite v6 raw counters, or backfill unknown history with invented dollar values. Acceptance: an approved design, isolated implementation, versioned tariff provenance, explicit unknown/unpriced handling, and tests for every supported billing formula.

## Later milestones: research gates

- [ ] **WD-013 - M3 guidance policy.** After WD-012, select calibrated rules for Codex coding sessions, cooldown/expiry, and provider-specific safe delivery. Acceptance: observe by default, opt-in, kill switch, and at least 90% precision with sample size disclosed; distinguish delivery from advice acceptance.
- [ ] **WD-014 - M4 LLM/control design.** After M3, decide CLI/API execution for Codex coding sessions, budget, isolation, and human-gate semantics. Acceptance: a separately accepted decision before implementation; checks against recursion and unintended data sharing.

## M-Anthropic - After WD-014, before WD-015

- [ ] **WD-022b - Claude enrichment, lifecycle, and guidance/control.** Scheduled after WD-014; builds on WD-022a. Add Claude transcript enrichment (versioned readers, offsets/rotation/partial tails) for both the session transcript and subagent transcripts (`agent_transcript_path`), usage reconciliation, the wider content-capture surface (`error`, `duration_ms`, `is_interrupt`), remaining CLI/desktop lifecycle checks beyond observation, supported guidance/control delivery, and — separately recorded — async hook delivery only after the synchronous baseline is fixed and recorded. Populate the v6 `model` and usage columns for Claude parent and subagent `usage` rows from `message.model` / `message.usage`, with `total_tokens` left `NULL`, and correlate each subagent row to its parent turn via `agent_id` and the parent `conversation_id`/`turn_id`; never source model from the `Agent` tool response. Acceptance: no cumulative usage double counting, parent-vs-subagent model resolvable per turn, mixed-provider isolation and bounded overhead, documented unavailable control capabilities, and durability of any backgrounded hook verified with the WD-022a evidence standard. No ordinary chats or Cowork. Does not block M1-M4; macOS/Linux validation remains WD-019.
  - Done (offline, branch `feature/wd-022b-claude-transcripts`): the daemon-only `claude-transcript-v1` reader for the session transcript and each subagent `agent_transcript_path`, sharing the Codex file-identity/offset/partial-tail/rotation/resume machinery. Grammar locked to Claude Code 2.1.259/2.1.260 by read-only inspection of real local transcripts: `message.usage` is per response and repeated on every content block, so one `usage` event is emitted per distinct `requestId`; each Anthropic counter (`input_tokens`, `cache_read_input_tokens`, `cache_creation_input_tokens`, `output_tokens`, `output_tokens_details.thinking_tokens`) is stored raw under `payload.claude.usage.response` with no synthesised total; `model` and `requestId` are carried on the envelope, `turn_id` stays `NULL` (assistant lines carry no `promptId` in these builds). Subagent rows are keyed `<parent session>#<agent_id>` in the `session_id` column (no schema bump; migrate to a real column when WD-111 owns v6) and emit `usage` rows resolving to the parent `session_id` with the subagent's `agent_id`. `build_envelope` now promotes `transcript_path`/`agent_transcript_path` for Claude; `purge_provider_session` clears the subagent reader rows; `error`/`is_interrupt`/`duration_ms` retention plus `error` redaction are covered. Verification: `uv run pytest` 346 passed, Ruff lint/format (src+tests), `uv run ty check`, `cargo fmt --check`, `cargo clippy --locked -- -D warnings`, `cargo build --release --locked`, `uv build`, `git diff --check`; `PRAGMA user_version` still 5. No provider probe; no live instance update yet.
  - Remaining: native trust/reload refresh and per-event capability gaps (item 1); WD-005 process-lifecycle checks for Claude CLI/desktop including child survival after harness exit (item 3); guidance/control delivery against M3/M4 and separately-recorded async hook delivery (item 5, blocked on WD-013/WD-014); the v6 `model`/usage column population (WD-111); live Windows evidence and the `LIVE.md` instance update.

## Optional extensions after M-Anthropic

- [ ] **WD-015 - App Server spike.** Scheduled after WD-022; not a dependency of M1-M4. Check ownership/attach to existing desktop/CLI sessions and available steer/interrupt operations. Acceptance: live evidence or an explicit limitation; add an adapter only if its benefit is confirmed.
- [ ] **WD-016 - Cross-project overview.** After a useful M2 release, evaluate a cross-project read-only view without merging databases and determine whether a web UI is needed.

## M5 - Deferred cross-platform validation

- [ ] **WD-019 - macOS/Linux host access and compatibility verification.** Obtain test hosts in the final milestone; verify package installation, hooks, detached launch, locks, path handling, and cleanup for both CLIs and officially available desktop surfaces. Reuse the Python core and existing probes. Acceptance: per-OS/version evidence and fixes for demonstrated differences, with unavailable surfaces explicitly marked. This task does not block WD-002, WD-008, M0-M4, or M-Anthropic; existing CI remains early feedback.

- [ ] **WD-026 - Resolve Codex hook performance on Windows.** Deferred until the end of the queue after WD-019. Investigate the external Windows/Codex command-runner cost that remains after the Rust adapter and shared-resolution optimization. The observed standalone CLI PowerShell contract is p95 279 ms sequential / 563 ms four-way; direct Rust is 65 / 173 ms, while a nested CMD `commandWindows` variation fails before handler start. Acceptance: either a reproducible vendor-supported launch path that meets the documented latency target under the actual Codex runner, or an evidence-backed external limitation/issue report with versioned measurements and a documented operational decision. Do not relax the historical WD-024 result retroactively.

## Post-MVP release engineering

- [ ] **WD-109 - GitHub Actions native adapter release artifacts.** Extend the existing Windows/Linux/macOS CI matrix into post-MVP release automation for the Rust hook adapter. Build the locked release binary for every declared OS/architecture target, run the native contract and spool-to-daemon-to-SQLite smoke checks on each supported runner, package artifacts with a stable provider-independent naming/version convention, and publish checksums (and signing metadata if releases require it). Verify that the hook installer can select the matching packaged binary without a locally installed Rust toolchain. Acceptance: a tagged workflow produces downloadable artifacts for every supported target, the installed-artifact smoke passes on each target, unsupported targets fail with an explicit diagnostic, and the workflow result is recorded as actual GitHub Actions evidence rather than inferred from configuration.

- [ ] **WD-110 - Remove the Python hook fallback.** Depends on WD-109. Make the packaged native adapter the only production hook entry point for the supported Windows/Linux/macOS matrix; do not silently select `src/agent_watchdog/hooks.py::observe` when a native executable is absent. Keep shared envelope construction, privacy, daemon, storage, and Python unit-test helpers that remain part of the core contract. Remove or explicitly mark fallback-only CLI/install options, tests, and documentation, and update the architecture/audit text to describe one delivery path. Acceptance: native-only hook installation and end-to-end delivery pass on every WD-109 target, missing or incompatible binaries produce a clear installation error, no fallback path is exercised by production hooks, and the Python fallback-specific behavior is either deleted or retained only as an explicitly non-production developer utility.

## Audit follow-ups

- [ ] **WD-101 - Test realism and boundary coverage.** Replace the exact command-list assertion in the CLI help test with a subprocess check that also proves an isolated `--home` remains untouched; fold the self-referential Claude fixture completeness check into the behavioral mapping test or tie it to an independently captured contract. Add a subprocess smoke for project registration, daemon lifecycle, and report; a Python hook -> daemon -> SQLite path in both content modes; and harmless argument round-trips through each supported shell available on the host. Keep mocked polling and synthetic stream tests as focused units, but do not present them as live evidence. Acceptance: focused and full offline suites pass, each new test states its evidence class, all owned processes/state are cleaned up, and no provider is invoked.
- [ ] **WD-102 - Manual and live evidence gates.** Define a versioned result record that separately reports provider dispatch, adapter start/failure, missing callback, retry count, persisted loss category, end-to-end delivery, and cleanup. Apply it to future isolated provider probes and retain provider/harness/OS versions plus sanitized provenance. Acceptance: synthetic, local adapter, and live-provider results cannot be conflated; every nonzero failure/loss is attributable or explicitly unknown; owned hooks, processes, and temporary state have recorded cleanup. No live probe is authorized by this backlog entry alone.

## Misc

- [] **WD-103 - manual/local gates in adapter (retries count, failures, etc.)** 
